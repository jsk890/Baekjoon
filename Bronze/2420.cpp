#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sync_with_stdio(false);
	/* 사파리월드 2420 */
	long long n, m;
	cin >> n >> m;
	cout << abs(n - m);
}